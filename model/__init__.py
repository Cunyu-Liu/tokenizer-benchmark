"""TokBench-RNA neural model package (Phase 3+).

Clean flat causal Transformer (F1-F7) and clean BLT backbone (P1-P3).
GPU-only execution enforced via device guard; cpu_fallback_count must be 0.
"""
__version__ = "0.3.0"
